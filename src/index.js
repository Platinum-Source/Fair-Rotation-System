import React from 'react';
import ReactDOM from 'react-dom/client';
import FairRotationSystem from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <FairRotationSystem />
  </React.StrictMode>
);